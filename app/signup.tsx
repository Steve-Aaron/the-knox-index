import { SignupPage } from '@/components/signup/SignupPage';

/**
 * /signup route
 * --------------
 * Public landing page for newsletter sign-ups.
 * All rendering is delegated to SignupPage so the route file
 * stays thin — routing config only.
 */

export default SignupPage;
